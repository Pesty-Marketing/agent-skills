Icon-only button for compact actions — menu toggles, close, prev/next.

```jsx
<IconButton icon="menu" label="Open menu" variant="ghost" />
<IconButton icon="arrow-right" variant="primary" round />
```

Shares Button's variant set. `round` makes it circular; default is a rounded square. Always pass `label` for accessibility.
