Circular avatar for team members and author bylines. Shows an image or falls back to initials.

```jsx
<Avatar src="/team/andrew.png" name="Andrew Peluso" size={64} ring />
<Avatar name="Dina Bickel" size={48} />
```

Use `ring` for the red brand ring (featured/CEO). Without `src` it renders initials on deep teal.
